# OpenStream V1.0.1 Setup

The current user-facing setup guide lives in [`docs/set-up.md`](set-up.md).

That guide includes screenshots, release download links, the recommended Windows installer path, and the manual plugin fallback. The Python receiver is a developer/debug path only and is not part of the normal user workflow.

For release packaging details, see [`docs/release.md`](release.md).
