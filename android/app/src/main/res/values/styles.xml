<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/os_accent</item>
        <item name="android:colorPrimaryDark">@color/os_status_bar</item>
        <item name="android:colorAccent">@color/os_accent</item>
        <item name="android:windowBackground">@color/os_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/os_black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- Brutalist dialog theme — transparent window bg so our custom layout's
         hard shadow shows properly against whatever is behind. -->
    <style name="BrutalistDialogTheme" parent="android:style/Theme.Material.Dialog.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.7</item>
    </style>
</resources>
